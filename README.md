# 🏥 RAG Medical Bot - MVP версия

Интеллектуальный ассистент для работы с медицинской документацией на основе RAG (Retrieval-Augmented Generation).

## 📋 Описание

RAG Medical Bot - это консольное приложение, которое позволяет:
- Индексировать текстовые медицинские документы
- Задавать вопросы на естественном языке
- Получать ответы с указанием источников информации
- Работать полностью локально без интернета

## 🎯 Основные возможности

- ✅ **Локальная работа** - все данные и модели работают на вашем компьютере
- ✅ **Русский язык** - полная поддержка русскоязычных документов
- ✅ **Векторный поиск** - быстрый поиск релевантной информации
- ✅ **Источники** - система показывает из каких документов взята информация
- ✅ **Персистентность** - индекс сохраняется и переиспользуется
- ✅ **Интерактивный интерфейс** - удобная работа через командную строку

## 🛠 Технический стек

- **LLM**: Llama 3.1 8B через Ollama (локально)
- **Embeddings**: intfloat/multilingual-e5-large (русскоязычная модель)
- **Vector Store**: ChromaDB (персистентное хранилище)
- **RAG Framework**: LlamaIndex
- **ML Framework**: PyTorch

## 📦 Системные требования

### Минимальные требования:
- **ОС**: Linux, macOS, Windows 10+
- **RAM**: 16 GB (рекомендуется 32 GB)
- **Диск**: 10 GB свободного места
- **Python**: 3.9+

### Рекомендуемые требования:
- **ОС**: Linux, macOS
- **RAM**: 32 GB
- **GPU**: NVIDIA с 8+ GB VRAM (опционально, для ускорения)
- **Python**: 3.10+

## 🚀 Установка

### Шаг 1: Установка Ollama

Ollama - это инструмент для запуска LLM моделей локально.

#### Linux:
```bash
curl -fsSL https://ollama.com/install.sh | sh
```

#### macOS:
```bash
brew install ollama
```

Или скачайте с официального сайта: https://ollama.com/download

#### Windows:
Скачайте установщик с https://ollama.com/download

### Шаг 2: Запуск Ollama

Запустите Ollama сервер:
```bash
ollama serve
```

Сервер будет доступен по адресу `http://localhost:11434`

### Шаг 3: Загрузка модели Llama 3.1 8B

В новом терминале выполните:
```bash
ollama pull llama3.1:8b
```

Это загрузит модель Llama 3.1 8B (~4.7 GB).

Проверьте, что модель загружена:
```bash
ollama list
```

### Шаг 4: Клонирование и установка проекта

```bash
# Перейдите в папку проекта
cd rag_mvp

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте виртуальное окружение
# Linux/macOS:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

**Примечание**: Установка может занять 5-10 минут, так как будут загружены PyTorch и другие большие библиотеки.

## 📖 Использование

### Базовый запуск

```bash
python main.py
```

### Первый запуск

При первом запуске программа:

1. Спросит путь к папке с документами (txt файлы)
2. Проиндексирует все документы
3. Сохранит индекс в папку `./storage`
4. Запустит интерактивный чат

### Последующие запуски

При повторном запуске программа:

1. Обнаружит существующий индекс
2. Спросит, использовать ли его или переиндексировать
3. Запустит интерактивный чат

### Интерактивные команды

В режиме чата доступны следующие команды:

- `/exit` - выход из программы
- `/reload` - переиндексация документов
- `/help` - справка по командам
- `/sources` - показать источники последнего ответа
- `/clear` - очистить экран

### Примеры вопросов

```
Ваш вопрос: Какие симптомы у гриппа?

Ваш вопрос: Какая дозировка парацетамола рекомендуется?

Ваш вопрос: Опиши методы диагностики диабета

Ваш вопрос: /sources  # Показать источники последнего ответа
```

## 📁 Структура проекта

```
rag_mvp/
├── main.py                 # Точка входа, интерактивный интерфейс
├── rag_engine.py          # Класс RAGEngine для работы с RAG
├── config.py              # Конфигурация системы
├── requirements.txt       # Python зависимости
├── README.md             # Документация (этот файл)
├── .gitignore            # Игнорируемые файлы
└── storage/              # Персистентное хранилище (создается автоматически)
    ├── chroma_db/        # ChromaDB векторная база
    └── metadata.txt      # Метаданные индекса
```

## ⚙️ Настройка

Все настройки находятся в файле `config.py`:

### Основные параметры:

```python
# Модели
OLLAMA_MODEL = "llama3.1:8b"
EMBEDDING_MODEL_NAME = "intfloat/multilingual-e5-large"

# Параметры индексации
CHUNK_SIZE = 512           # Размер чанков
CHUNK_OVERLAP = 50         # Перекрытие чанков

# Параметры поиска
SIMILARITY_TOP_K = 3       # Количество релевантных чанков

# Параметры генерации
TEMPERATURE = 0.1          # Температура LLM (0.0-1.0)
MAX_TOKENS = 1024          # Максимальная длина ответа
```

Вы можете изменить эти параметры в `config.py` для настройки под свои нужды.

## 🔧 Troubleshooting

### Проблема: "Не удалось подключиться к Ollama"

**Решение**:
1. Убедитесь, что Ollama запущена: `ollama serve`
2. Проверьте, что сервер доступен: `curl http://localhost:11434`
3. Проверьте, что модель загружена: `ollama list`

### Проблема: "Модель llama3.1:8b не найдена"

**Решение**:
```bash
ollama pull llama3.1:8b
```

### Проблема: "Out of memory" при индексации

**Решение**:
1. Уменьшите `CHUNK_SIZE` в `config.py` (например, до 256)
2. Уменьшите `SIMILARITY_TOP_K` (например, до 2)
3. Обрабатывайте документы небольшими партиями

### Проблема: "ModuleNotFoundError"

**Решение**:
```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate     # Windows

# Переустановите зависимости
pip install -r requirements.txt
```

### Проблема: Медленная работа

**Решения**:
1. **Используйте GPU** (если доступна):
   - В `rag_engine.py` измените `device="cpu"` на `device="cuda"`
2. **Уменьшите параметры**:
   - `SIMILARITY_TOP_K = 2` (меньше контекста)
   - `MAX_TOKENS = 512` (короткие ответы)
3. **Используйте меньшую модель**:
   - `ollama pull llama3.1:3b` (меньше, но быстрее)

### Проблема: Ответы не на русском языке

**Решение**:
1. Убедитесь, что используется правильная модель: `llama3.1:8b`
2. Проверьте `SYSTEM_PROMPT` в `config.py` - он должен быть на русском
3. Формулируйте вопросы на русском языке

### Проблема: Индекс не сохраняется

**Решение**:
1. Проверьте права доступа к папке `storage/`
2. Убедитесь, что есть свободное место на диске
3. Проверьте логи на наличие ошибок сохранения

## 🎨 Продвинутое использование

### Использование GPU для ускорения

Если у вас есть NVIDIA GPU:

1. Установите CUDA версию PyTorch:
```bash
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
```

2. В `rag_engine.py` измените:
```python
self.embed_model = HuggingFaceEmbedding(
    model_name=config.EMBEDDING_MODEL_NAME,
    device="cuda"  # Было "cpu"
)
```

### Добавление других форматов документов

По умолчанию поддерживаются только `.txt` файлы. Для добавления других форматов:

1. Установите дополнительные библиотеки:
```bash
pip install pypdf python-docx  # PDF и DOCX
```

2. Модифицируйте метод `index_documents()` в `rag_engine.py`

### Настройка промптов

Промпты настраиваются в `config.py`:

```python
SYSTEM_PROMPT = """
Ваш кастомный системный промпт...
"""
```

## 📊 Производительность

Типичные показатели на различном железе:

| Конфигурация | Индексация (100 док) | Ответ на вопрос |
|-------------|---------------------|-----------------|
| CPU (16GB RAM) | ~2-3 минуты | ~5-10 секунд |
| CPU (32GB RAM) | ~1-2 минуты | ~3-5 секунд |
| GPU (RTX 3060) | ~30-60 секунд | ~2-3 секунды |
| GPU (RTX 4090) | ~20-30 секунд | ~1-2 секунды |

## 🤝 Вклад в проект

Это MVP версия. Приветствуются:
- Отчеты об ошибках
- Предложения по улучшению
- Pull requests

## 📄 Лицензия

MIT License - свободное использование для любых целей.

## ⚠️ Дисклеймер

Этот бот предназначен для помощи в работе с медицинской документацией, но **НЕ ЯВЛЯЕТСЯ** заменой профессиональной медицинской консультации. Всегда консультируйтесь с квалифицированными медицинскими специалистами.

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте раздел Troubleshooting
2. Убедитесь, что все зависимости установлены
3. Проверьте, что Ollama запущена и модель загружена

---

**Версия**: 1.0.0 (MVP)
**Дата обновления**: 2026-01-10
