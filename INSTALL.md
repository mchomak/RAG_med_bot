# 🚀 Быстрая установка RAG Medical Bot

## Шаг 1: Установка Ollama

### Windows:
1. Скачайте установщик: https://ollama.com/download/windows
2. Запустите установщик и следуйте инструкциям
3. Откройте командную строку и выполните:
```cmd
ollama serve
```

### Linux:
```bash
curl -fsSL https://ollama.com/install.sh | sh
ollama serve
```

### macOS:
```bash
brew install ollama
ollama serve
```

## Шаг 2: Загрузка модели

В новом терминале:
```bash
ollama pull llama3.1:8b
```

Проверка:
```bash
ollama list
```

## Шаг 3: Установка Python зависимостей

### Windows:
```cmd
cd rag_mvp
python -m venv venv
venv\Scripts\activate
pip install --upgrade pip
pip install -r requirements.txt
```

### Linux/macOS:
```bash
cd rag_mvp
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

**Важно**: Установка может занять 10-15 минут из-за загрузки PyTorch (~2GB) и других больших библиотек.

## Шаг 4: Запуск

```bash
python main.py
```

## ⚠️ Решение проблем

### Проблема: ImportError при запуске

**Симптомы**:
```
ImportError: cannot import name 'ToolCallBlock' from 'llama_index.core.base.llms.types'
```

**Решение**:
```bash
# Удалите виртуальное окружение
rm -rf venv  # Linux/macOS
rmdir /s venv  # Windows

# Создайте заново
python3 -m venv venv  # Linux/macOS
python -m venv venv   # Windows

# Активируйте
source venv/bin/activate  # Linux/macOS
venv\Scripts\activate     # Windows

# Установите заново с правильными версиями
pip install --upgrade pip
pip install -r requirements.txt
```

### Проблема: Ollama не запускается

**Решение**:
```bash
# Проверьте, не запущен ли уже
curl http://localhost:11434

# Если не отвечает, запустите
ollama serve
```

### Проблема: Модель не найдена

**Решение**:
```bash
ollama pull llama3.1:8b
ollama list  # Проверка
```

### Проблема: Недостаточно памяти

**Решение**:
1. Закройте другие программы
2. Или используйте меньшую модель:
```bash
ollama pull llama3.1:3b
```
И измените в `config.py`:
```python
OLLAMA_MODEL = "llama3.1:3b"
```

## 📊 Проверка установки

После установки выполните:
```bash
python main.py
```

Если всё работает, вы увидите приветственный баннер:
```
╔═══════════════════════════════════════════════════════════╗
║          🏥 RAG Medical Bot - MVP версия 🏥             ║
╚═══════════════════════════════════════════════════════════╝
```

## 💡 Полезные команды

```bash
# Обновить зависимости
pip install --upgrade -r requirements.txt

# Проверить версию Python
python --version  # Должно быть 3.9+

# Проверить установленные пакеты
pip list | grep llama-index

# Очистить кэш pip (если проблемы с установкой)
pip cache purge
```

## 📞 Дополнительная помощь

Если проблемы остались:
1. Проверьте README.md - раздел "Troubleshooting"
2. Убедитесь, что Python 3.9+ установлен
3. Убедитесь, что есть ~5GB свободного места на диске
4. Проверьте, что виртуальное окружение активировано

---

**Готово!** Теперь можно использовать систему. Укажите путь к папке с документами и начните задавать вопросы.
